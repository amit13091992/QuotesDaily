package com.amit.quotesdaily.activity;import android.content.Intent;import android.content.SharedPreferences;import android.os.Build;import android.os.Bundle;import android.support.annotation.RequiresApi;import android.support.v4.content.ContextCompat;import android.support.v7.app.AppCompatActivity;import android.support.v7.widget.CardView;import android.util.Log;import android.view.View;import android.view.Window;import android.view.WindowManager;import com.amit.quotesdaily.R;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStreamReader;import java.nio.charset.StandardCharsets;import java.util.ArrayList;@RequiresApi(api = Build.VERSION_CODES.LOLLIPOP)public class CategoryActivity extends AppCompatActivity implements View.OnClickListener {    ArrayList<String> quoteList = new ArrayList<>();    ArrayList<String> quoteTitleList = new ArrayList<>();    CardView lifestyleCV, motivationCV, positivityCV, inspirationCV, selfawareCV, successCV, timeCV, loveCV;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_category);        if (getSupportActionBar() != null) {            getSupportActionBar().hide();        }        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) {            Window window = getWindow();            window.addFlags(WindowManager.LayoutParams.FLAG_DRAWS_SYSTEM_BAR_BACKGROUNDS);            window.setStatusBarColor(ContextCompat.getColor(getApplicationContext(), R.color.colorPrimaryDark));        }        //getQuotesFromFile();        lifestyleCV = (CardView) findViewById(R.id.idCV_lifestyle);        motivationCV = (CardView) findViewById(R.id.idCV_motivation);        positivityCV = (CardView) findViewById(R.id.idCV_positivity);        inspirationCV = (CardView) findViewById(R.id.idCV_inspiration);        selfawareCV = (CardView) findViewById(R.id.idCV_humor);        //successCV = (CardView) findViewById(R.id.idCV_success);        timeCV = (CardView) findViewById(R.id.idCV_time);        loveCV = (CardView) findViewById(R.id.idCV_love);        setViewclickListner();    }    private void setViewclickListner() {        lifestyleCV.setOnClickListener(this);        motivationCV.setOnClickListener(this);        positivityCV.setOnClickListener(this);        inspirationCV.setOnClickListener(this);        selfawareCV.setOnClickListener(this);        //successCV.setOnClickListener(this);        timeCV.setOnClickListener(this);        loveCV.setOnClickListener(this);    }//    /**//     * Method to read file and getting text and title from file from /assets folder.//     *//     * @return//     *///    @RequiresApi(api = Build.VERSION_CODES.LOLLIPOP)//    private void getQuotesFromFile() {//        String filename = "famousquotes.txt";//        BufferedReader bufferedReader = null;//        String subString = null;//        quoteList.clear();//        quoteTitleList.clear();////        try {//            bufferedReader = new BufferedReader(new InputStreamReader//                    (this.getAssets().open(filename), StandardCharsets.UTF_8));//            String line;//            while ((line = bufferedReader.readLine()) != null) {//                String[] parts = line.split(" "); // String array, each element is text between//                String title = parts[0];    // Text before the first dot//                quoteTitleList.add(title);//                String s1 = line.substring(line.indexOf(" ") + 1);//                s1.trim();//                quoteList.add(s1);//            }//            prefrenceManager.putListString("quoteList", quoteList);//            prefrenceManager.putListString("quoteTitleList", quoteTitleList);////            Log.e("quote list size: ---> ", String.valueOf(quoteTitleList.size()));//            Log.e("List: ", quoteList.size() + quoteList.toString() + "\n");//            Log.e("Title List: ", quoteTitleList.size() + quoteTitleList.toString());////        } catch (IOException e) {//            e.printStackTrace();//        } finally {//            if (bufferedReader != null) {//                try {//                    bufferedReader.close();//                } catch (IOException e) {//                    e.printStackTrace();//                }//            }//        }//    }    @Override    public void onClick(View view) {        //switch (view.getId()) {        if (view.getId() == R.id.idCV_lifestyle) {            readFileData("Lifestyle");            //launchMainActivity("Lifestyle");        } else if (view.getId() == R.id.idCV_motivation) {            //launchMainActivity("Motivation");            readFileData("Motivation");        } else if (view.getId() == R.id.idCV_positivity) {            //launchMainActivity("Positivity");            readFileData("Positivity");        } else if (view.getId() == R.id.idCV_inspiration) {            //launchMainActivity("Inspiration");            readFileData("Inspiration");        } else if (view.getId() == R.id.idCV_humor) {            //launchMainActivity("Humor");            readFileData("Humor");        }//        else if (view.getId() == R.id.idCV_success) {//            readFileData("Success");//            //launchMainActivity("Success");//        }        else if (view.getId() == R.id.idCV_time) {            //launchMainActivity("Time");            readFileData("Time");        } else if (view.getId() == R.id.idCV_love) {            //launchMainActivity("Love");            readFileData("Love");        }    }    private void readFileData(String filename) {        BufferedReader reader = null;        quoteList.clear();        try {            reader = new BufferedReader(                    new InputStreamReader(getApplicationContext().getAssets().open(filename + ".txt"),                            StandardCharsets.UTF_8));            String mLine;            while ((mLine = reader.readLine()) != null) {                quoteList.add(mLine);            }        } catch (IOException e) {            //log the exception        } finally {            if (reader != null) {                try {                    reader.close();                } catch (IOException e) {                    //log the exception                }            }        }        Log.d("File name: --- ", filename);        startMainActivity(filename);    }    private void startMainActivity(String title) {        Intent intent = new Intent(CategoryActivity.this, MainActivity.class);        intent.putExtra("quote_text", quoteList);        intent.putExtra("quote_title", title);        startActivity(intent);    }//    private void launchMainActivity(String string) {//        List<String> tempTitleList = new ArrayList<>();//        List<String> tempIndex = new ArrayList<>();//        for (String temp : quoteTitleList) {//            if (temp.contains(string)) {//                tempTitleList.add(temp);//                tempIndex.add(String.valueOf(quoteTitleList.indexOf(string)));//                int indx = quoteTitleList.indexOf(quoteTitleList);//                Log.e(" title index: ", String.valueOf(indx));//                Log.e(" title index llists: ", tempIndex.toString());//            }//        }//        Map<ArrayList<String>, ArrayList<String>> m = new HashMap<ArrayList<String>, ArrayList<String>>();//        m.put(quoteTitleList, quoteList);//        for (Map.Entry<ArrayList<String>, ArrayList<String>> entry : m.entrySet()) {//            quoteTitleList = entry.getKey();//            quoteList = entry.getValue();//            //Log.e("map List--- key: ", quoteTitleList.toString());//            //Log.e("map List--- value: ", quoteList.toString());////            if (quoteTitleList.contains(string)){//                System.out.print("key value:--->>> " + m.values());//            }//        }////        Log.e("filtered Title List : ", tempTitleList.size() + tempTitleList.toString());//    }}